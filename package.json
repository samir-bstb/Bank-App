{
  "name": "bank-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "mobile": "pnpm --filter @bank-app/mobile start",
    "web": "pnpm --filter @bank-app/web dev",
    "build:web": "pnpm --filter @bank-app/web build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
